{
  "name": "pr-refresh",
  "version": "1.0.0",
  "description": "GitHub Action to refresh all open PRs when main branch is updated",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "keywords": [
    "github",
    "actions",
    "status",
    "check"
  ],
  "author": "Debuide Org",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
